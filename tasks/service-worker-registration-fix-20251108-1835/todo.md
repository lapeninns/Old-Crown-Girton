# Implementation Checklist

- [x] Correct `CACHE_DURATIONS` object in `public/sw.js` (remove stray `\n`, ensure proper key).
- [x] Search for other `\n` literals outside template strings in `public/sw.js`.
- [x] Run dev server and confirm no registration errors in console via DevTools MCP.
